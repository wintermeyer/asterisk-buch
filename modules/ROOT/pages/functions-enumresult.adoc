[[functions-enumresult]]
= `ENUMRESULT()`

[source]
----
ENUMRESULT(id,nummer)
----

Gibt ein Ergebnis aus einer ENUM-Ergebnismenge zurück, siehe
`ENUMQUERY()` (xref:functions-enumquery.adoc[Abschnitt D.28, „`ENUMQUERY()`“]). Als
_`id`_ übergibt man die durch
`DUNDIQUERY()` erhaltene ID der Ergebnismenge.
_`nummer`_ ist die Nummer des abzufragenden
Ergebnisses aus der Menge, wobei die Zählung bei 1 beginnt. Wenn man für
_`nummer`_ `getnum` übergibt, erhält
man die Anzahl der Treffer.

.Asterisk-Versionen:
| | | | | *1.6* |————————

.Interner Hilfetext zu dieser Funktion in Asterisk 1.6:
-= Info about function 'ENUMRESULT' =-

[Syntax]
ENUMRESULT(id,resultnum)

[Synopsis]
Retrieve results from a ENUMQUERY

[Description]
This function will retrieve results from a previous use
of the ENUMQUERY function.
id - This argument is the identifier returned by the ENUMQUERY function.
resultnum - This is the number of the result that you want to retrieve.
Results start at 1. If this argument is specified as "getnum",
then it will return the total number of results that are available.

.Differenz des internen Hilfetexts von Asterisk 1.4 zu 1.6:
— in Asterisk 1.4 nicht vorhanden —

.Siehe auch
xref:functions-enumquery.adoc[Abschnitt D.28, „`ENUMQUERY()`“], xref:functions-enumlookup.adoc[Abschnitt D.27, „`ENUMLOOKUP()`“], `enum.conf`, xref:functions-dundiresult.adoc[Abschnitt D.26, „`DUNDIRESULT()`“]
